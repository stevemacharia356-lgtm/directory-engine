---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import PhoneButton from '../../components/PhoneButton.astro';
import WhatsAppButton from '../../components/WhatsAppButton.astro';
import WrittenDirections from '../../components/WrittenDirections.astro';
import MapFacade from '../../components/MapFacade.astro';
import { getAllBusinessSlugs, getBusinessData, getAllDirectorySlugs, getDirectoryData } from '../../lib/firestore';

export async function getStaticPaths() {
  const slugs = await getAllBusinessSlugs();
  return slugs.map(slug => ({ params: { business: slug } }));
}

const { business } = Astro.params;
const data = await getBusinessData(business!);

if (!data) {
  return Astro.redirect('/404');
}

// Get related directories for internal linking
const allDirs = await getAllDirectorySlugs();
const relatedDirs = [];
for (const dirSlug of allDirs) {
  const dirData = await getDirectoryData(dirSlug);
  if (dirData && data.location && dirData.locationDisplay?.toLowerCase() === data.location.toLowerCase()) {
    relatedDirs.push(dirData);
  }
}

const pageTitle = data.name + ' — ' + (data.tagline || 'Welcome');
const pageDescription = data.description?.substring(0, 160) || '';

// JSON-LD schemas
const localBusinessSchema = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: data.name,
  description: data.description,
  image: data.heroImage || undefined,
  telephone: data.phone || undefined,
  address: data.location ? {
    '@type': 'PostalAddress',
    addressLocality: data.location,
    addressCountry: 'KE'
  } : undefined,
  geo: data.coordinates ? {
    '@type': 'GeoCoordinates',
    latitude: data.coordinates.lat,
    longitude: data.coordinates.lng
  } : undefined,
  openingHours: data.openingHours ? [
    data.openingHours.weekdays ? 'Mo-Fr ' + data.openingHours.weekdays : null,
    data.openingHours.saturday ? 'Sa ' + data.openingHours.saturday : null,
    data.openingHours.sunday ? 'Su ' + data.openingHours.sunday : null
  ].filter(Boolean) : undefined,
  priceRange: data.priceRange || undefined
};

const faqSchema = data.faqs && data.faqs.length > 0 ? {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: data.faqs.map(faq => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer }
  }))
} : null;

const articleSchema = data.blog?.title ? {
  '@context': 'https://schema.org',
  '@type': 'Article',
  headline: data.blog.title,
  dateModified: data.blog.lastModified,
  author: { '@type': 'Organization', name: 'Kenya Accommodation Directory' },
  mainEntityOfPage: { '@type': 'WebPage', '@id': `${Astro.url.origin}/business/${business}/` }
} : null;
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  ogImage={data.heroImage}
  canonicalURL={`${Astro.url.origin}/business/${business}/`}
  geoCoordinates={data.coordinates}
  geoPlacename={data.location ? data.location + ', Kenya' : 'Kenya'}
>
  <slot name="head">
    <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema)} />
    {faqSchema && <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />}
    {articleSchema && <script type="application/ld+json" set:html={JSON.stringify(articleSchema)} />}
  </slot>

  <Header breadcrumbs={[{ label: 'Home', url: '/' }, { label: data.name }]} />

  <main>
    <section style="text-align:center; padding:2rem 0;">
      {data.heroImage && (
        <img src={data.heroImage} alt={data.heroImageAlt || data.name} width="800" height="400" loading="eager" style="border-radius:8px; width:100%; max-height:400px; object-fit:cover;" />
      )}
      <h1 style="font-size:2rem; margin:1rem 0 0.5rem;">{data.name}</h1>
      {data.tagline && <p style="font-size:1.2rem; color:#666; font-style:italic;">{data.tagline}</p>}
      {data.category && <span style="background:#0066cc; color:white; padding:0.3rem 1rem; border-radius:20px; font-size:0.9rem;">{data.category}</span>}
    </section>

    <div style="display:flex; gap:1rem; justify-content:center; flex-wrap:wrap; margin:1.5rem 0;">
      {data.phone && <PhoneButton phone={data.phone} display={data.phoneDisplay || data.phone} />}
      {data.whatsapp && <WhatsAppButton number={data.whatsapp} message={data.whatsappMessage || 'Hello, I visited your website.'} />}
    </div>

    {data.openingHours && (
      <section style="background:#f0f4ff; padding:1rem; border-radius:8px; margin:1rem 0; text-align:center;">
        <h3 style="margin-top:0; color:#003d80;">🕐 Opening Hours</h3>
        <p style="margin:0.25rem 0;"><strong>Mon-Fri:</strong> {data.openingHours.weekdays || 'N/A'}</p>
        <p style="margin:0.25rem 0;"><strong>Saturday:</strong> {data.openingHours.saturday || 'N/A'}</p>
        <p style="margin:0.25rem 0;"><strong>Sunday:</strong> {data.openingHours.sunday || 'N/A'}</p>
      </section>
    )}

    {data.description && (
      <section style="background:#f8f8f8; padding:1.5rem; border-radius:8px; margin:1.5rem 0;">
        <h2 style="color:#333; margin-top:0;">About {data.name}</h2>
        <p style="font-size:1.1rem; line-height:1.8;">{data.description}</p>
      </section>
    )}

    {data.services && data.services.length > 0 && (
      <section style="margin:1.5rem 0;">
        <h2 style="color:#333;">What We Offer</h2>
        <div style="display:flex; flex-wrap:wrap; gap:0.75rem;">
          {data.services.map((s: string) => (
            <span style="background:#e8f4e8; padding:0.5rem 1rem; border-radius:20px; font-size:1rem;">✓ {s}</span>
          ))}
        </div>
      </section>
    )}

    {data.photos && data.photos.length > 0 && (
      <section style="margin:1.5rem 0;">
        <h2 style="color:#333;">Gallery</h2>
        <div style="display:grid; grid-template-columns:repeat(auto-fit, minmax(250px, 1fr)); gap:0.5rem;">
          {data.photos.map((photo: any) => (
            <img src={photo.src} alt={photo.alt || data.name} width="400" height="300" loading="lazy" style="border-radius:4px; width:100%; object-fit:cover;" />
          ))}
        </div>
      </section>
    )}

    {data.directions && <WrittenDirections directions={data.directions} />}
    {data.coordinates && <MapFacade lat={data.coordinates.lat} lng={data.coordinates.lng} name={data.name} />}

    {data.faqs && data.faqs.length > 0 && (
      <section style="margin:1.5rem 0;">
        <h2 style="color:#333;">Frequently Asked Questions</h2>
        {data.faqs.map((faq: any) => (
          <details>
            <summary>{faq.question}</summary>
            <p style="padding:0.5rem 1rem;">{faq.answer}</p>
          </details>
        ))}
      </section>
    )}

    {data.blog?.title && (
      <section style="background:#fff8f0; padding:1.5rem; border-radius:8px; border-left:6px solid #ff8c00; margin:2rem 0;">
        <h2 style="color:#804000; margin-top:0;">📝 {data.blog.title}</h2>
        <time datetime={data.blog.lastModified} style="font-size:0.8rem; color:#999;">
          🕐 Last updated: {new Date(data.blog.lastModified).toLocaleDateString('en-KE', { year: 'numeric', month: 'long', day: 'numeric' })}
        </time>
        <div style="margin-top:1rem; line-height:1.8;">
          <Fragment set:html={data.blog.bodyHtml || ''} />
        </div>
      </section>
    )}

    {relatedDirs.length > 0 && (
      <section style="background:#f0f4ff; padding:1.5rem; border-radius:8px; margin:2rem 0;">
        <h2 style="color:#003d80; margin-top:0;">Explore More in {data.location}</h2>
        <div style="display:flex; flex-wrap:wrap; gap:0.5rem;">
          {relatedDirs.map((dir: any) => (
            <a href={`/${dir.directory}/`} style="display:inline-block; padding:0.5rem 1rem; background:white; border:1px solid #0066cc; color:#0066cc; text-decoration:none; border-radius:20px; font-size:0.9rem;">
              {dir.nicheDisplay} in {dir.locationDisplay}
            </a>
          ))}
        </div>
      </section>
    )}

    <div style="text-align:center; margin:2rem 0;">
      <a href="/" style="display:inline-block; padding:0.75rem 1.5rem; background:#0066cc; color:white; text-decoration:none; border-radius:4px; font-weight:bold;">
        ← Back to Home
      </a>
    </div>
  </main>

  <Footer />
</BaseLayout>